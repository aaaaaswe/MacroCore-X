# MacroCore-X v0.0.1 指令集架构规范

**版本**：0.0.1  
**状态**：草案（Draft）  
**目标**：通用桌面/服务器64位处理器  
**编码**：变长（2/4/6/8字节）  
**字节序**：小端（Little-Endian）

---

## 第一章 整体架构

### 1.0 模块化架构

MacroCore-X v1.0 采用**模块化分层**设计：

**核心层（MacroCore-X，必选）** — 任何实现都必须包含：
- **R型**：标量整数运算（add, sub, mul, div, and, or, xor, shl, shr 等）
- **I型**：立即数运算（addi, subi, muli, movi, mov 等）
- **L型**：加载/存储（ld, st, ldw, stw, ldb, stb 等）
- **B型**：分支与跳转（beq, bne, jmp, call, ret, jreg 等）
- **C型**：复合操作（addm, subm, xchg, push, pop, enter, leave 等）
- **系统型**：系统控制与调试（syscall, int, cpuid, hlt, cli, sti, ecall, fence, bkpt 等）

**扩展层（可选）** — 按需裁剪：
- **F型（标量浮点扩展，0xA0–0xAF）**：fadd, fsub, fmul, fdiv, fcmp, fsqrt, fcvt, fmin, fmax, fneg, fabs
- **V型（向量扩展）**：vadd, vsub, vmul, vld, vst, vshuffle, vfmadd 等
- **矩阵加速扩展（预留）**：mmul, macc 等

> 层级关系：MacroCore-X 核心层 = 系统控制 + 标量整数 + 内存 + 分支 + 复合操作 + 调试，必选；扩展层 = 标量浮点 / 向量 / 矩阵，可选。

### 1.1 执行状态

| 组件 | 数量 | 宽度 | 说明 |
|------|------|------|------|
| 通用寄存器（GR） | 32 | 64位 | R0–R31 |
| 浮点寄存器（FR） | 32 | 64位 | F0–F31 |
| 向量寄存器（VR） | 32 | 256位 | V0–V31 |
| 程序计数器（PC） | 1 | 64位 | 指向当前指令首字节 |
| 栈指针（SP） | 1 | 64位 | 软件约定使用R2，非硬连线 |
| 标志寄存器（FLAGS） | 1 | 4位 | CF/ZF/SF/OF |
| 模式寄存器（MODE） | 1 | 64位 | 特权级、向量长度、对齐策略等 |

### 1.2 寄存器详细定义

**R0**：硬连线为零。写入被忽略，读取返回0。  
**R1–R7**：参数传递（调用者保存）。  
**R8–R15**：临时/局部变量（调用者保存）。  
**R16–R23**：被调用者保存（Callee-Saved）。  
**R24–R30**：通用保留。  
**R31**：**返回地址寄存器（RA）**。`call`/`callreg`自动写入PC+当前指令长度；`ret`自动跳转到R31内容。

> 注：SP由软件约定为R2，但ISA不做硬编码，允许编译器自由选择栈指针寄存器。

### 1.3 浮点寄存器

F0–F31，64位宽，独立的标量浮点寄存器文件。F 型指令（fadd、fsub、fmul 等）对 F 寄存器执行 IEEE 754 标量浮点运算。F 寄存器与 V 寄存器（向量寄存器）完全独立，两者互不干扰。

### 1.4 向量寄存器

V0–V31，256位宽，可存放：
- 4个64位整数/浮点
- 8个32位整数/浮点
- 16个16位整数
- 32个8位整数

向量操作按**元素宽度**由指令后缀/编码决定。

---

## 第二章 指令编码

### 2.1 长度判定（硬解码规则）

每条指令首字节的**高2位**决定指令总长度：

| `byte0[7:6]` | 指令长度 | 类别区间 |
|--------------|----------|----------|
| `00` | 4字节 | R型（操作码0x00–0x1F） |
| `01` | 4/6字节 | I/L/B型（操作码0x20–0x7F） |
| `10` | 6/8字节 | V/C/F扩展型（操作码0x80–0xBF） |
| `11` | 8字节 | 长指令（操作码0xC0–0xFF） |

**解码器必须在首周期仅根据 byte0 的 bits[7:6] 确定指令长度，无需解析后续字节。**

> 注：在 `01` 区间中，`movi`（0x2A）和部分 L 型索引变体（0x50–0x5F）为 6 字节。解码器在初始长度分类后，通过完整操作码字节区分这些情况。在 `10` 区间中，系统型指令（0xB0–0xBF）为 2 或 4 字节。

### 2.2 指令字段定义

| 字段名 | 缩写 | 宽度（位） | 说明 |
|--------|------|-----------|------|
| 操作码 | OP | 2–8 | 指令功能编码 |
| 目标寄存器 | Rd | 5 | 通用寄存器编号（0–31） |
| 源寄存器1 | Rs1 | 5 | 通用寄存器编号 |
| 源寄存器2 | Rs2 | 5 | 通用寄存器编号 |
| 立即数 | IMM | 5–32 | 符号或零扩展 |
| 偏移量 | OFF | 12–21 | 相对跳转/内存偏移 |
| 扩展标志 | X | 1–4 | 宽度/原子/条件修饰 |

### 2.3 操作码映射表（完整）

| 操作码区间 | 指令类别 | 长度 | 主要用途 |
|-----------|----------|------|----------|
| 0x00–0x1F | **R型** | 4字节 | 寄存器-寄存器运算 |
| 0x20–0x3F | **I型** | 4/6字节 | 立即数运算/加载 |
| 0x40–0x5F | **L型** | 4/6字节 | 加载/存储 |
| 0x60–0x7F | **B型** | 4字节 | 分支/跳转 |
| 0x80–0x8F | **V型** | 6/8字节 | 向量扩展（可选） |
| 0x90–0x9F | **C型** | 6/8字节 | 复合存储器操作 |
| 0xA0–0xAF | **F型** | 6字节 | 标量浮点扩展（可选） |
| 0xB0–0xBF | **系统型** | 2/4字节 | 特权/管理 |
| 0xC0–0xFF | **保留** | 各长度 | 未来扩展 |

---

## 第三章 指令定义

### 3.1 R型指令（4字节，OP 0x00–0x1F）

**格式（4字节）**：
```
位:    7..0         7..0         7..0         7..0
       ┌───────────┬───────────┬───────────┬───────────┐
字节0   │ OP[7:0]               │   完整操作码 0x00–0x1F
       ├───────────┼───────────┼───────────┼───────────┤
字节1   │ Rd[4:0]       │ Rs1[4:2]      │   Rd（5位）+ Rs1 高3位
       ├───────────┼───────────┼───────────┼───────────┤
字节2   │ Rs1[1:0]  │ Rs2[4:0]      │X│   Rs1 低2位 + Rs2（5位）+ X
       ├───────────┼───────────┼───────────┼───────────┤
字节3   │ 保留（0x00）                        │
       └───────────┴───────────┴───────────┴───────────┘
```

- **Rd**：目标寄存器（5位，0–31）
- **Rs1**：源寄存器1（5位，0–31）
- **Rs2**：源寄存器2（5位，0–31）
- **X**：扩展标志（保留供未来使用，必须为 0）

R型使用显式三操作数格式：Rd、Rs1、Rs2 均独立指定。
编码：byte0 = 完整操作码，byte1 = (Rd<<3) | (Rs1>>2)，byte2 = ((Rs1&3)<<6) | (Rs2<<1) | X。

#### R型指令列表

| OP | 助记符 | 操作数 | 语义 |
|----|--------|--------|------|
| 0x00 | `add` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] + R[Rs2] |
| 0x01 | `sub` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] - R[Rs2] |
| 0x02 | `mul` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] × R[Rs2]（低64位） |
| 0x03 | `div` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] ÷ R[Rs2]（有符号，向零舍入） |
| 0x04 | `divu` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] ÷ R[Rs2]（无符号） |
| 0x05 | `and` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] & R[Rs2] |
| 0x06 | `or` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] \| R[Rs2] |
| 0x07 | `xor` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] ^ R[Rs2] |
| 0x08 | `shl` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] << (R[Rs2] & 0x3F) |
| 0x09 | `shr` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] >> (R[Rs2] & 0x3F)（逻辑） |
| 0x0A | `sar` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] >> (R[Rs2] & 0x3F)（算术） |
| 0x0B | `eq` | Rd, Rs1, Rs2 | R[Rd] ← (R[Rs1] == R[Rs2]) ? 1 : 0 |
| 0x0C | `lt` | Rd, Rs1, Rs2 | R[Rd] ← (R[Rs1] < R[Rs2]) ? 1 : 0（有符号） |
| 0x0D | `ltu` | Rd, Rs1, Rs2 | R[Rd] ← (R[Rs1] < R[Rs2]) ? 1 : 0（无符号） |
| 0x0E | `max` | Rd, Rs1, Rs2 | R[Rd] ← max(R[Rs1], R[Rs2])（有符号） |
| 0x0F | `min` | Rd, Rs1, Rs2 | R[Rd] ← min(R[Rs1], R[Rs2])（有符号） |
| 0x10 | `ror` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] 循环右移 (R[Rs2] & 0x3F) |
| 0x11 | `rol` | Rd, Rs1, Rs2 | R[Rd] ← R[Rs1] 循环左移 (R[Rs2] & 0x3F) |
| 0x12 | `clz` | Rd, Rs1 | R[Rd] ← 前导零计数（R[Rs1]） |

> 所有R型指令更新标志位：CF/ZF/SF/OF（算术/逻辑指令）；`eq`/`lt`/`ltu`仅更新ZF；`clz`不更新标志。

---

### 3.2 I型指令（4/6字节，OP 0x20–0x3F）

**格式（4字节）**：
```
字节0: [OP 8位]
字节1: [Rd[4:0] 5位][Rs1[4:2] 3位]
字节2: [Rs1[1:0] 2位][IMM[13:8] 6位]
字节3: [IMM[7:0] 8位]
```
IMM 为14位立即数（byte2[5:0]||byte3[7:0]），大多数指令将其符号扩展至64位。

**格式（6字节，仅 `movi`）**：
```
字节0: [OP 8位 = 0x2A]
字节1: [Rd[4:0] 5位][000 3位]
字节2-5: IMM32（小端，零扩展）
```

| OP | 助记符 | 操作数 | 语义 |
|----|--------|--------|------|
| 0x20 | `addi` | Rd, Rs1, imm14 | R[Rd] ← R[Rs1] + sext(imm14) |
| 0x21 | `subi` | Rd, Rs1, imm14 | R[Rd] ← R[Rs1] - sext(imm14) |
| 0x22 | `muli` | Rd, Rs1, imm14 | R[Rd] ← R[Rs1] × sext(imm14) |
| 0x23 | `andi` | Rd, Rs1, imm14 | R[Rd] ← R[Rs1] & zext(imm14) |
| 0x24 | `ori` | Rd, Rs1, imm14 | R[Rd] ← R[Rs1] \| zext(imm14) |
| 0x25 | `xori` | Rd, Rs1, imm14 | R[Rd] ← R[Rs1] ^ zext(imm14) |
| 0x26 | `shli` | Rd, Rs1, imm6 | R[Rd] ← R[Rs1] << imm6（IMM[5:0]） |
| 0x27 | `shri` | Rd, Rs1, imm6 | R[Rd] ← R[Rs1] >> imm6（逻辑） |
| 0x28 | `sari` | Rd, Rs1, imm6 | R[Rd] ← R[Rs1] >> imm6（算术） |
| 0x29 | `mov` | Rd, imm14 | R[Rd] ← sext(imm14) |
| 0x2A | `movi` | Rd, imm32 | R[Rd] ← zext(imm32)（6字节扩展） |
| 0x2B-0x3F | *保留* | — | — |

> 标志更新规则同R型。

---

### 3.3 L型指令（加载/存储，4/6字节，OP 0x40–0x5F）

**格式（4字节）**：
```
字节0: [OP 8位]          （完整操作码 0x40–0x46）
字节1: [Rd 4位][Rs1 4位]
字节2-3: OFF16（小端有符号偏移）
```

> 注：L-type、B-type 条件分支和 C-type 指令使用 4 位寄存器字段（Rd、Rs1、Rs2），仅能访问 R0–R15。R-type 和 I-type 指令使用 5 位寄存器字段，可访问全部 32 个寄存器（R0–R31）。

**格式（6字节索引寻址，OP 0x50–0x5F）**：
```
字节0-1: 同上
字节2-3: OFF16
字节4-5: Rn（索引寄存器编号）+ 缩放因子（2位）
```

加载/存储宽度由操作码决定：`ld`/`st` = 64位，`ldu`/`lds` = 32位（符号/零扩展），`stw` = 32位，`stb` = 8位。

| OP | 助记符 | 操作数 | 语义 |
|----|--------|--------|------|
| 0x40 | `ld` | Rd, [Rs1 + off] | R[Rd] ← zext(Mem[Rs1+off, 64]) |
| 0x41 | `ldu` | Rd, [Rs1 + off] | R[Rd] ← zext(Mem[Rs1+off, 32]) |
| 0x42 | `lds` | Rd, [Rs1 + off] | R[Rd] ← sext(Mem[Rs1+off, 32]) |
| 0x43 | `st` | Rs1, [Rs2 + off] | Mem[Rs2+off, 64] ← R[Rs1] |
| 0x44 | `stw` | Rs1, [Rs2 + off] | Mem[Rs2+off, 32] ← R[Rs1]（低32位） |
| 0x45 | `stb` | Rs1, [Rs2 + off] | Mem[Rs2+off, 8] ← R[Rs1]（低8位） |
| 0x46 | `lda` | Rd, Rs1, Rs2, scale | R[Rd] ← R[Rs1] + R[Rs2] × scale（scale=1/2/4/8） |

> 注：`lda` 复用 OFF16 字段（字节2-3）编码 Rs2 和 scale：字节2-3 = (Rs2 << 2) | scale_bits，其中 scale_bits = 0/1/2/3 对应 scale 1/2/4/8。
| 0x50 | `ldr` | Rd, [Rs1 + Rn*scale + off] | R[Rd] ← Mem[Rs1 + Rn×scale + off, 64]（6字节） |
| 0x51 | `str` | Rs1, [Rs2 + Rn*scale + off] | Mem[Rs2 + Rn×scale + off, 64] ← R[Rs1]（6字节） |
| 0x52-0x5F | *保留* | — | — |

> L型指令不更新标志位。

---

### 3.4 B型指令（分支/跳转，4字节，OP 0x60–0x7F）

**格式（条件分支，OP 0x63–0x6A）**：
```
字节0:   [OP 8位]          （完整操作码 0x63–0x6A）
字节1:   [Rs1 4位][Rs2 4位]
字节2-3: IMM12（小端有符号偏移）
```

**格式（`j`/`call`，OP 0x60/0x61）**：
```
字节0:   [OP 8位]          （完整操作码 0x60/0x61）
字节1:   imm20[19:12]
字节2-3: imm20[11:0]（小端有符号偏移）
```

**偏移计算**：`target = PC + sign_ext(IMM12) << 2`（条件分支）  
`target = PC + sign_ext(IMM20) << 2`（`j`/`call`，使用全部20位）

| OP | 助记符 | 操作数 | 语义 |
|----|--------|--------|------|
| 0x60 | `j` | target20 | PC ← PC + sext(imm20)<<2 |
| 0x61 | `call` | target20 | R31 ← PC + 4; PC ← PC + sext(imm20)<<2 |
| 0x62 | `ret` | — | PC ← R[31] |
| 0x63 | `beq` | Rs1, Rs2, target12 | if (R[Rs1] == R[Rs2]) PC ← PC + sext(imm12)<<2 |
| 0x64 | `bne` | Rs1, Rs2, target12 | if (R[Rs1] != R[Rs2]) PC ← PC + sext(imm12)<<2 |
| 0x65 | `blt` | Rs1, Rs2, target12 | if (R[Rs1] < R[Rs2]) PC ← PC + sext(imm12)<<2（有符号） |
| 0x66 | `ble` | Rs1, Rs2, target12 | if (R[Rs1] ≤ R[Rs2]) PC ← PC + sext(imm12)<<2（有符号） |
| 0x67 | `bgt` | Rs1, Rs2, target12 | if (R[Rs1] > R[Rs2]) PC ← PC + sext(imm12)<<2（有符号） |
| 0x68 | `bge` | Rs1, Rs2, target12 | if (R[Rs1] ≥ R[Rs2]) PC ← PC + sext(imm12)<<2（有符号） |
| 0x69 | `bltu` | Rs1, Rs2, target12 | if (R[Rs1] < R[Rs2]) PC ← PC + sext(imm12)<<2（无符号） |
| 0x6A | `bgeu` | Rs1, Rs2, target12 | if (R[Rs1] ≥ R[Rs2]) PC ← PC + sext(imm12)<<2（无符号） |
| 0x6B | `jreg` | Rs1 | PC ← R[Rs1] |
| 0x6C | `callreg` | Rs1 | R31 ← PC + 4; PC ← R[Rs1] |
| 0x6D-0x6F | *保留* | — | — |

> 注意：`call`和`callreg`写入R31时，保存的返回地址是**当前PC + 4**（即`call`指令本身4字节长度），因为所有B型指令都是4字节。`ret`不检查R[31]有效性，由软件保证。

---

### 3.5 F型指令（标量浮点扩展，可选，6字节，OP 0xA0–0xAF）

**格式（6字节）**：
```
字节0:   [0xA 4位][Fd 4位]
字节1:   [Fs1 4位][Fs2 4位]
字节2:   FUNCT8（操作码）
字节3:   AUX  [rm 3位][prec 2位][rsv 3位]
字节4-5: 保留
```

- **Fd**：目标 F 寄存器（标量浮点结果，0–31）
- **Fs1, Fs2**：源 F 寄存器（标量浮点操作数，0–31）
- **FUNCT8**：操作码
- **AUX**：精度和舍入模式
  - `prec`（位[2:1]）：0 = f32，1 = f64
  - `rm`（位[6:3]）：舍入模式（0 = RNE，1 = RTZ，2 = RDN，3 = RUP）

#### F型寄存器模型：独立浮点寄存器文件

F型标量浮点指令使用**独立的 F 寄存器文件**（F0–F31，每个64位）：

- F 寄存器文件包含 32 个 64 位寄存器，专门用于标量浮点运算。
- F 型指令仅操作 F 寄存器，与 V 寄存器（向量寄存器，256位）**完全独立**。
- 浮点数据在 F 寄存器和内存之间通过 `fld`/`fst` 指令传输，或在整数和浮点之间通过 `fcvt` 指令转换。
- 此设计避免寄存器文件复用，支持标量浮点与向量运算真正并行执行。

> 独立 FPU 设计：F 寄存器和 V 寄存器各自独立，F 型指令不依赖 V 寄存器，V 型指令不依赖 F 寄存器。编译器可以自由分配两种寄存器，硬件可以实现独立的浮点和向量执行单元。

| FUNCT | 助记符 | 操作数 | 语义 |
|----|--------|--------|------|
| 0x00 | `fadd` | Fd, Fs1, Fs2 | F[Fd] ← float(F[Fs1]) + float(F[Fs2]) |
| 0x01 | `fsub` | Fd, Fs1, Fs2 | F[Fd] ← float(F[Fs1]) - float(F[Fs2]) |
| 0x02 | `fmul` | Fd, Fs1, Fs2 | F[Fd] ← float(F[Fs1]) × float(F[Fs2]) |
| 0x03 | `fdiv` | Fd, Fs1, Fs2 | F[Fd] ← float(F[Fs1]) ÷ float(F[Fs2]) |
| 0x04 | `fsqrt` | Fd, Fs1 | F[Fd] ← sqrt(float(F[Fs1])) |
| 0x05 | `fcmp` | Fs1, Fs2 | ZF ← (F[Fs1]==F[Fs2]); CF ← (F[Fs1]<F[Fs2]) |
| 0x06 | `fcvt.w.s` | Fd, Fs1 | F[Fd] ← int(float(F[Fs1]))（向零截断） |
| 0x07 | `fcvt.s.w` | Fd, Fs1 | F[Fd] ← float(int(F[Fs1])) |
| 0x08 | `fmin` | Fd, Fs1, Fs2 | F[Fd] ← min(float(F[Fs1]), float(F[Fs2])) |
| 0x09 | `fmax` | Fd, Fs1, Fs2 | F[Fd] ← max(float(F[Fs1]), float(F[Fs2])) |
| 0x0A | `fneg` | Fd, Fs1 | F[Fd] ← -float(F[Fs1]) |
| 0x0B | `fabs` | Fd, Fs1 | F[Fd] ← abs(float(F[Fs1])) |
| 0x0C | `fld` | Fd, [Rs1 + off] | F[Fd] ← Mem[Rs1+off, 64] |
| 0x0D | `fst` | Fd, [Rs1 + off] | Mem[Rs1+off, 64] ← F[Fd] |
| 0x0E-0xFF | *保留* | — | — |

> F[Fd] 表示 F 寄存器文件中的第 Fd 号寄存器（64位）。

#### 3.5.1 浮点状态寄存器（CSR_FSR，0x00A）

FSR 捕获浮点异常标志：

| 位 | 名称 | 说明 |
|-----|------|------|
| 0 | NV | 无效操作（例如 0/0、sqrt(-1)、∞−∞） |
| 1 | DZ | 除零 |
| 2 | OF | 上溢（结果过大） |
| 3 | UF | 下溢（结果过小，非规格化） |
| 4 | NX | 不精确（结果已舍入） |
| 7:5 | Rsv | 保留 |

- FSR 标志是**粘滞的**——一旦置位，将保持置位状态，直到通过 `wrmsr` 向对应位写入 0 来清除。
- 浮点异常默认**不会**触发 #FP 异常。软件必须轮询 CSR_FSR 或通过未来的 CSR_FCR（浮点控制寄存器）配置捕获。
- 当浮点操作产生 NaN、无穷大或非规格化数时，相应的 FSR 标志被置位，操作以 IEEE 754 默认结果继续执行。

---

### 3.6 V型指令（向量扩展，可选，6/8字节，OP 0x80–0x8F）

**格式（6字节）**：
```
字节0:   [0x8 4位][Vd 4位]
字节1:   [Vs1 4位][Vs2 4位]
字节2:   FUNCT8（操作码）
字节3:   AUX（元素宽度 / 掩码 / 混洗控制）
字节4-5: EXT（vld/vst的偏移量，vfmadd的Vs3）
```

**格式（8字节融合乘加）**：
```
字节0-5: 同上
字节4-5: Vs3（vfmadd的第三个源寄存器）
```

所有向量指令元素宽度由AUX字节的低2位指定：
- `00` = 8位
- `01` = 16位
- `10` = 32位
- `11` = 64位

| FUNCT | 助记符 | 操作数 | 语义 |
|----|--------|--------|------|
| 0x00 | `vadd` | Vd, Vs1, Vs2 | V[Vd][i] ← V[Vs1][i] + V[Vs2][i] |
| 0x01 | `vsub` | Vd, Vs1, Vs2 | V[Vd][i] ← V[Vs1][i] - V[Vs2][i] |
| 0x02 | `vmul` | Vd, Vs1, Vs2 | V[Vd][i] ← V[Vs1][i] × V[Vs2][i] |
| 0x03 | `vand` | Vd, Vs1, Vs2 | V[Vd][i] ← V[Vs1][i] & V[Vs2][i] |
| 0x04 | `vor` | Vd, Vs1, Vs2 | V[Vd][i] ← V[Vs1][i] \| V[Vs2][i] |
| 0x05 | `vxor` | Vd, Vs1, Vs2 | V[Vd][i] ← V[Vs1][i] ^ V[Vs2][i] |
| 0x06 | `vld` | Vd, [Rs1 + off16] | V[Vd] ← Mem[Rs1+off, 256]（对齐） |
| 0x07 | `vst` | Vd, [Rs1 + off16] | Mem[Rs1+off, 256] ← V[Vd]（对齐） |
| 0x08 | `vshl` | Vd, Vs1, imm5 | V[Vd][i] ← V[Vs1][i] << imm5（逐元素） |
| 0x09 | `vshr` | Vd, Vs1, imm5 | V[Vd][i] ← V[Vs1][i] >> imm5（逻辑） |
| 0x0A | `vshuffle` | Vd, Vs1, imm8 | V[Vd][i] ← V[Vs1][ imm8[i] ]（按字节打乱，8字节） |
| 0x0B | `vfmadd` | Vd, Vs1, Vs2, Vs3 | V[Vd][i] ← V[Vs1][i]×V[Vs2][i]+V[Vs3][i]（8字节） |
| 0x0C-0xFF | *保留* | — | — |

#### 3.6.1 向量掩码

向量指令通过 AUX 字节（byte3）支持逐元素掩码：

- **AUX[3]**（掩码使能）：0 = 无掩码（所有元素活跃），1 = 掩码使能
- **AUX[2]**（掩码极性）：0 = 掩码=0 表示元素不活跃，1 = 掩码=0 表示元素活跃
- 当掩码使能时，**掩码寄存器**隐式为 **V0**。V0 寄存器低 N 位（N = 256 / 元素宽度）的每个位控制目标对应元素是否被写入。
  - 掩码位 = 0：元素**不活跃**（目标元素保留，被掩码元素不产生异常）
  - 掩码位 = 1：元素**活跃**（正常操作）
- 掩码极性（AUX[2]）可反转此行为：0 = 活跃，1 = 不活跃。

> 示例：对于32位元素的向量操作（8个元素），V0[7:0] 作为掩码位。若 V0 = 0x0000...0005（位0和位2置位），则仅元素0和元素2活跃。

---

### 3.7 C型指令（复合CISC，6/8字节，OP 0x90–0x9F）

所有C型指令在微架构层面分解为多个µop，但在ISA层面保证**原子性**（除特别注明）。

| OP | 助记符 | 操作数 | 语义 | µop分解 |
|----|--------|--------|------|---------|
| 0x90 | `addm` | Rs1, [Rs2 + off] | Mem[Rs2+off] ← Mem[Rs2+off] + R[Rs1] | ld→add→st |
| 0x91 | `subm` | Rs1, [Rs2 + off] | Mem[Rs2+off] ← Mem[Rs2+off] - R[Rs1] | ld→sub→st |
| 0x92 | `xchg` | Rs1, [Rs2 + off] | 交换 R[Rs1] ↔ Mem[Rs2+off]（原子） | ld→st |
| 0x93 | `cmpxchg` | Rs1, Rs2, [Rs3+off] | if (Mem[Rs3+off] == R[Rs1]) Mem←R[Rs2]（原子，8字节） | ld→cmp→st |
| 0x94 | `push` | Rs1 | SP←SP-8; Mem[SP,64] ← R[Rs1] | sub→st |
| 0x95 | `pop` | Rs1 | R[Rs1] ← Mem[SP,64]; SP←SP+8 | ld→add |
| 0x96 | `enter` | imm16 | SP←SP-imm16; Mem[SP,64]←R[30]（保存RBP） | sub→st |
| 0x97 | `leave` | — | SP←R[30]+8; R[30]←Mem[SP-8,64] | ld→add |
| 0x98-0x9F | *保留* | — | — |

> `push`/`pop`使用**R2作为SP**（约定），`enter`使用R30作为帧指针（约定），但ISA不强制。

---

### 3.8 系统指令（2/4字节，OP 0xB0–0xBF）

| OP | 助记符 | 操作数 | 语义 | 长度 |
|----|--------|--------|------|------|
| 0xB0 | `syscall` | imm8 | 触发系统调用（imm8→系统调用号，进入内核态） | 2 |
| 0xB1 | `sysret` | — | 从系统调用返回（恢复用户态） | 2 |
| 0xB2 | `int` | imm8 | 软件中断（imm8→中断向量） | 2 |
| 0xB3 | `iret` | — | 从中断返回 | 2 |
| 0xB4 | `rdmsr` | Rs1, imm12 | R[Rs1] ← MSR[imm12] | 4 |
| 0xB5 | `wrmsr` | Rs1, imm12 | MSR[imm12] ← R[Rs1] | 4 |
| 0xB6 | `cpuid` | — | 填充R0:R1:R2:R3 = 厂商/特性/缓存/版本信息 | 2 |
| 0xB7 | `hlt` | — | 暂停执行，直到中断 | 2 |
| 0xB8 | `cli` | — | 清除中断使能标志（IF=0） | 2 |
| 0xB9 | `sti` | — | 设置中断使能标志（IF=1） | 2 |
| 0xBA | `nop` | — | 空操作（PC ← PC + 2） | 2 |
| 0xBB | `ecall` | imm8 | 环境调用（模拟器/调试入口） | 2 |
| 0xBC | `fence` | — | 内存栅栏/屏障（参见 §5.3） | 2 |
| 0xBD | `bkpt` | imm8 | 硬件断点（调试） | 2 |
| 0xBE-0xBF | *保留* | — | — | 各长度 |

---

## 第四章 异常与中断

### 4.1 异常向量表

| 向量号 | 名称 | 触发条件 |
|--------|------|----------|
| 0x00 | 除零异常 | `div`/`divu` 除数为0 |
| 0x01 | 非法指令 | 未定义操作码 |
| 0x02 | 对齐错误 | 未对齐访问且CSR_MODE.ALIGN=1 |
| 0x03 | 页错误 | MMU缺页/权限违规 |
| 0x04 | 断点 | `ecall` 指令 |
| 0x05 | 系统调用 | `syscall` 指令 |
| 0x06 | 外部中断 | 硬件IRQ |
| 0x07-0x1F | 保留 | 自定义 |

### 4.2 异常处理流程

1. 保存当前PC到**异常返回寄存器（ERR）**（内部不可见）；
2. 保存当前FLAGS到**EF（异常标志）**；
3. 进入特权模式（CSR_MODE.PRIV=0）；
4. PC ← 异常向量表基址 + 向量号×8；
5. 执行异常处理程序。

`iret` 指令从异常返回：恢复FLAGS ← EF，PC ← ERR。

### 4.3 控制与状态寄存器（CSR）

CSR 通过 `rdmsr`（0xB4）和 `wrmsr`（0xB5）指令访问。CSR 地址空间为 12 位（0x000–0xFFF）。

| CSR# | 名称 | 宽度 | 说明 |
|------|------|------|------|
| 0x000 | `CSR_ERR` | 64 | 异常返回PC（异常时保存，`iret` 恢复） |
| 0x001 | `CSR_EF` | 8 | 异常标志（异常时保存的 FLAGS） |
| 0x002 | `CSR_MODE` | 64 | 模式寄存器：PRIV[0], ALIGN[1], VLEN[3:2], 保留[63:4] |
| 0x003 | `CSR_CR3` | 64 | 页表基地址（根页表的物理地址） |
| 0x004 | `CSR_IVEC` | 64 | 异常向量表基地址（默认：0x0000） |
| 0x005 | `CSR_IE` | 32 | 中断使能掩码（每位对应一个中断，参见 §4.4） |
| 0x006 | `CSR_IP` | 32 | 中断挂起寄存器（只读） |
| 0x007 | `CSR_IPI` | 64 | 核间中断：写入以向目标核心发送 IPI |
| 0x008 | `CSR_TIMER` | 64 | 定时器计数器（以固定频率递增） |
| 0x009 | `CSR_TIMECMP` | 64 | 定时器比较：当 CSR_TIMER >= CSR_TIMECMP 时触发中断 |
| 0x00A | `CSR_FSR` | 8 | 浮点状态寄存器（参见 §3.5.1） |
| 0x00B | `CSR_DBGCTL` | 64 | 调试控制寄存器（参见 §4.5） |
| 0x00C–0x01F | `CSR_PMC0`–`CSR_PMC19` | 64 | 性能监控计数器 0–19（参见 §4.6） |
| 0x020–0x03F | *保留* | — | 未来标准 CSR |
| 0x040–0xFFF | *厂商定义* | — | 实现特定 CSR |

#### CSR 字段详情

**CSR_MODE（0x002）**：
- 位 [0]：PRIV — 0 = 内核，1 = 用户
- 位 [1]：ALIGN — 0 = 允许非对齐访问，1 = 非对齐时触发异常
- 位 [3:2]：VLEN — 向量长度：00 = 128位，01 = 256位，10 = 512位，11 = 保留
- 位 [63:4]：保留（必须为 0）

**CSR_CR3（0x003）**：
- 位 [63:12]：根页表的物理页号（PPN）
- 位 [11:0]：保留（必须为 0，确保 4KB 对齐）

### 4.4 中断控制器

#### 4.4.1 中断号

| IRQ# | 类型 | 说明 |
|------|------|------|
| 0 | 定时器 | 定时器中断（CSR_TIMER >= CSR_TIMECMP） |
| 1 | IPI | 核间中断（来自其他核心） |
| 2–15 | 外部 | 外部硬件 IRQ 线（平台定义） |
| 16–31 | 软件 | 软件生成中断（`int` 指令） |

#### 4.4.2 中断控制

- **CSR_IE**（0x005）：每个位控制对应中断是否使能（1 = 使能）。位 0 = 定时器，位 1 = IPI，位 2–15 = 外部 IRQ。
- **CSR_IP**（0x006）：只读寄存器，指示挂起的中断。位布局与 CSR_IE 一致。
- **CSR_IPI**（0x007）：写入 (core_id << 16) | (vector & 0xF) 向指定核心发送 IPI。接收核心将看到 CSR_IP 位 1 置位。

#### 4.4.3 中断优先级

优先级由高到低：
1. 机器检查（不可屏蔽）
2. 外部中断（IRQ 2–15）—— IRQ# 越高优先级越高
3. 定时器（IRQ 0）
4. IPI（IRQ 1）
5. 软件中断（IRQ 16–31）

### 4.5 调试接口

#### 4.5.1 调试模式入口

- **硬件断点**：`bkpt imm8` 指令（0xBD），若 CSR_DBGCTL[0]（DBE）= 1，则触发进入调试模式。
- **单步执行**：当 CSR_DBGCTL[1]（SSE）= 1 时，CPU 在执行每条指令后进入调试模式。
- 调试模式将 PC 保存到 CSR_ERR，FLAGS 保存到 CSR_EF，然后跳转到 CSR_DBGCTL[63:12] << 12 处的调试处理程序。

#### 4.5.2 调试控制寄存器（CSR_DBGCTL，0x00B）

| 位 | 名称 | 说明 |
|-----|------|------|
| 0 | DBE | 调试使能（1 = 响应 `bkpt`） |
| 1 | SSE | 单步执行使能 |
| 63:12 | DBGHADDR | 调试处理程序基地址（页对齐） |

### 4.6 性能计数器（PMC）

20 个性能监控计数器（CSR_PMC0–CSR_PMC19，CSR 0x00C–0x01F），用于硬件性能监控：

| CSR# | 计数器 | 默认事件 |
|------|--------|----------|
| 0x00C | PMC0 | 已退休指令数 |
| 0x00D | PMC1 | CPU 周期数 |
| 0x00E | PMC2 | 分支指令数 |
| 0x00F | PMC3 | 分支预测失败数 |
| 0x010 | PMC4 | L1 数据缓存访问次数 |
| 0x011 | PMC5 | L1 数据缓存未命中次数 |
| 0x012 | PMC6 | L1 指令缓存访问次数 |
| 0x013 | PMC7 | L1 指令缓存未命中次数 |
| 0x014 | PMC8 | TLB 未命中次数 |
| 0x015 | PMC9 | 页错误次数 |
| 0x016 | PMC10 | 浮点操作次数 |
| 0x017 | PMC11 | 向量操作次数 |
| 0x018 | PMC12 | 存储缓冲区满停顿次数 |
| 0x019 | PMC13 | 加载-使用互锁停顿次数 |
| 0x01A–0x01F | PMC14–PMC19 | 实现定义 |

> 所有 PMC 均为 64 位回绕计数器。可通过 `rdmsr` 读取，通过 `wrmsr` 写入 0 来重置。

---

## 第五章 内存模型

### 5.1 地址空间

- 64位平坦地址空间，虚拟地址宽**48位**（当前实现），物理地址宽**52位**（可配置）。
- 支持4KB、2MB、1GB页。

### 5.2 页表格式

MacroCore-X 使用 4 级层次化页表（类似 x86_64 和 RISC-V Sv39/Sv48）。

#### 5.2.1 页表项（PTE）格式

每个 PTE 为 8 字节（64位）：

```
位:   63:52         51:12          11:9  8   7   6   5   4   3   2   1   0
      ┌─────────────┬───────────────┬─────┬───┬───┬───┬───┬───┬───┬───┬───┐
      │ 保留        │ PPN[51:12]    │ Rsv │ G │ U │ A │ D │ W │ X │ R │ V │
      └─────────────┴───────────────┴─────┴───┴───┴───┴───┴───┴───┴───┴───┘
```

| 位 | 名称 | 说明 |
|-----|------|------|
| 0 | V | 有效 — 1 = PTE 有效 |
| 1 | R | 可读 — 1 = 允许读访问 |
| 2 | X | 可执行 — 1 = 允许执行访问 |
| 3 | W | 可写 — 1 = 允许写访问 |
| 4 | D | 脏位 — 首次写入页面时由硬件置位 |
| 5 | A | 访问位 — 首次访问页面时由硬件置位 |
| 6 | U | 用户 — 1 = 用户模式（PRIV=1）可访问 |
| 7 | G | 全局 — 1 = 全局映射（TLB 上下文切换时不刷新） |
| 11:8 | Rsv | 保留供未来使用 |
| 51:12 | PPN | 物理页号（4KB 页为 40 位） |
| 63:52 | Rsv | 保留（必须为 0） |

#### 5.2.2 页大小

| 页大小 | 使用的 PPN 位 | PTE 层级 | 标识 |
|--------|---------------|----------|------|
| 4KB | PPN[51:12] | 第 0 级（叶子） | 标准页 |
| 2MB | PPN[51:21] | 第 1 级（叶子） | 大页（PTE[51:21]） |
| 1GB | PPN[51:30] | 第 2 级（叶子） | 巨页（PTE[51:30]） |

大页/巨页通过在对应层级设置叶子 PTE（而非指向下一级的指针）来标识。

#### 5.2.3 虚拟地址分解（4KB 页）

```
位:   63:48         47:39       38:30       29:21       20:12       11:0
      ┌─────────────┬───────────┬───────────┬───────────┬───────────┬──────┐
      │ 符号扩展    │ VPN[3]    │ VPN[2]    │ VPN[1]    │ VPN[0]    │ Off  │
      └─────────────┴───────────┴───────────┴───────────┴───────────┴──────┘
         (16位)       (9位)       (9位)       (9位)       (9位)       (12位)
```

- VPN[3] 索引第 3 级（根）页表
- VPN[2] 索引第 2 级页表
- VPN[1] 索引第 1 级页表
- VPN[0] 索引第 0 级（叶子）页表
- CSR_CR3 指向第 3 级页表的物理基地址

### 5.3 对齐策略

CSR_MODE.ALIGN 位控制：
- `0`：允许非对齐访问（由微架构处理）
- `1`：非对齐访问触发 #对齐异常

### 5.4 内存排序

**弱内存模型（Weak Memory Model）**——不同内存访问之间不保证顺序，除非显式强制。

#### 5.4.1 fence 指令

**格式（2字节）**：
```
字节0: 0xBC
字节1: [PI 4位][PO 4位]
```

- **PI**（前驱输入）：必须在 fence 之前完成的内存操作
- **PO**（前驱输出）：必须在 fence 之前完成的内存操作
- `PI`/`PO` 编码：位 0 = 加载，位 1 = 存储，位 2–3 = 保留

| 编码 | 助记符 | 语义 |
|------|--------|------|
| `fence`（PI=0xF, PO=0xF） | 全屏障 | 所有之前的加载和存储完成后，才允许后续任何加载/存储 |
| `fence w, w`（PI=0x2, PO=0x2） | 存储-存储屏障 | 所有之前的存储完成后，才允许后续存储 |
| `fence r, r`（PI=0x1, PO=0x1） | 加载-加载屏障 | 所有之前的加载完成后，才允许后续加载 |
| `fence rw, rw`（PI=0x3, PO=0x3） | 加载+存储屏障 | 所有之前的加载/存储完成后，才允许后续加载/存储 |

#### 5.4.2 原子指令与内存排序

- **`xchg`**（0x92）：隐式**全内存屏障**（获取 + 释放）。等效于指令中内嵌 `fence rw, rw`。
- **`cmpxchg`**（0x93）：隐式**全内存屏障**。加载-比较和条件存储阶段相对于其他内存操作是原子的。
- **`addm`** / **`subm`**（0x90–0x91）：在 ISA 层面**不是原子的**（分解为 ld→alu→st µop）。使用 `cmpxchg` 循环实现原子读-修改-写。

> 设计理由：`xchg` 和 `cmpxchg` 提供隐式全屏障（类似 x86 `lock` 前缀），以简化无锁编程。需要较弱排序的软件应使用常规加载/存储配合显式 `fence` 指令。

---

## 第六章 汇编语法

### 6.1 语法风格（类AT&T，但简化）

```
<指令> <目标>, <源1>, <源2>
```

**示例**：
```
add r1, r1, r2       # R型：r1 = r1 + r2
addi r3, r1, 0x100    # I型：r3 = r1 + 256
ld r4, [r2 + 0x8]     # 加载64位
st r5, [r3 - 0x4]     # 存储64位
call 0x10000          # 函数调用
fadd f2, f0, f1       # F型标量浮点加法
vadd v1, v2, v3       # V型向量加法
push r6               # 压栈
syscall 0x1           # 系统调用
fence                 # 全内存屏障
bkpt 0                # 断点，调试码为0
```

### 6.2 伪指令

| 伪指令 | 展开 |
|--------|------|
| `li Rd, imm` | `mov Rd, imm`（若imm≤14位）或`movi Rd, imm`（若imm>14位） |
| `la Rd, label` | `lda Rd, PC, label, 1`（地址计算） |
| `nop` | `add r0, r0`（或有独立`nop`指令） |

---

## 第七章 与现有生态的对接

### 7.1 ELF文件格式

需要定义：
- **EM_MACROCORE** 机器码（申请值待定）
- **重定位类型**：R_MACRO_32、R_MACRO_64、R_MACRO_PC20、R_MACRO_PC12
- **ABI版本**：宏内核（Linux风格）或微内核

### 7.2 系统调用ABI

约定（与Linux x86_64兼容简化版）：
- 系统调用号：R1
- 参数1–6：R2–R7
- **返回值**：R1（成功 = 0 或正数；失败 = 负 errno，例如 -ENOMEM）
- **错误指示**：返回值在 [-4095, -1] 范围内表示错误。其绝对值为 errno。
- **调用者保存寄存器**：R1–R15（可能被内核修改）
- **被调用者保存寄存器**：R16–R23, R30–R31（跨系统调用保留）
- **R0**：始终为 0（硬连线）
- **SP（R2）**：应保持有效；内核可能访问用户栈以处理某些系统调用（如 `execve`）
- 系统调用指令：`syscall 0x0`（使用 R1 中的系统调用号）
- 当前版本不支持 vsyscall/vDSO；未来版本可能通过内核系统调用页实现快速系统调用。

#### 标准系统调用号

| # | 名称 | R2 | R3 | R4 | 说明 |
|---|------|----|----|----|------|
| 0 | `exit` | status | — | — | 终止进程 |
| 1 | `write` | fd | buf | count | 写入文件描述符 |
| 2 | `read` | fd | buf | count | 从文件描述符读取 |
| 3 | `open` | path | flags | mode | 打开文件 |
| 4 | `close` | fd | — | — | 关闭文件描述符 |
| 5 | `mmap` | addr | len | prot/flags/fd/off | 映射内存 |
| 6 | `munmap` | addr | len | — | 取消内存映射 |
| 7 | `brk` | addr | — | — | 更改程序断点 |
| 8 | `fork` | — | — | — | 创建子进程 |
| 9 | `execve` | path | argv | envp | 执行程序 |
| 10 | `waitpid` | pid | status | options | 等待子进程 |
| 11–63 | *保留* | — | — | — | POSIX 兼容 |
| 64–255 | *自定义* | — | — | — | 实现定义 |

---

## 附录A：指令编码完整表（字节级，部分）

| 助记符 | 字节0（hex） | 长度 | 格式 |
|--------|-------------|------|------|
| `add` | 0x00 | 4 | R |
| `sub` | 0x01 | 4 | R |
| `mul` | 0x02 | 4 | R |
| `div` | 0x03 | 4 | R |
| `divu` | 0x04 | 4 | R |
| ...（完整表略） | | | |
| `syscall` | 0xB0 | 2 | SYS |
| `cpuid` | 0xB6 | 2 | SYS |
| `nop` | 0xBA | 2 | SYS |
| `fence` | 0xBC | 2 | SYS |
| `bkpt` | 0xBD | 2 | SYS |

> 完整编码表需配合操作数编码器生成，本文档提供权威参考。

---

## 附录B：设计决策记录

| 决策 | 理由 |
|------|------|
| 2/4/6/8字节变长 | 解码器首周期通过高2位定长，兼顾密度与解码速度 |
| R0硬连线0 | 简化编译器（常见零值）和通用代码模式 |
| R31硬件返回地址 | 去掉`jalr`的额外指令，`call`/`ret`更高效 |
| R型4字节（v2.1） | 从2字节扩展以支持完整32寄存器文件（5位字段）；解决v2.0密度与寄存器数量的矛盾 |
| F型移至0xA0–0xAF（v2.1） | 从0x70–0x7F移至`10`区间（6字节区），以维持"高2位=长度"的硬解码规则 |
| F型使用独立F寄存器文件 | 标量浮点与向量运算完全解耦，支持真正并行执行；F寄存器与V寄存器各自独立，不再复用 |
| 弱内存模型 | 适用于多核；`fence`和`xchg`/`cmpxchg`中的隐式屏障提供顺序控制 |
| 原子指令隐式全屏障 | `xchg`/`cmpxchg`包含全内存屏障（类似x86 `lock`），简化无锁编程 |
| 4级页表 | 兼容Linux内核页表遍历器；48位虚拟地址，52位物理地址 |
| 基于CSR的系统控制 | 统一的CSR空间用于ERR、MODE、CR3、FSR、PMC等；通过`rdmsr`/`wrmsr`访问 |

---

## 附录C：交付物（工具链）

1. **ISA规范**（本文档）
2. **汇编器**（`assembler.py`）—— 汇编到二进制
3. **模拟器**（`simulator.py`）—— 行为模拟
4. **GNU Binutils `.md` 文件**（`gas` 移植的操作码描述）—— 待定
5. **QEMU 目标描述框架**（用于全系统模拟）—— 待定
